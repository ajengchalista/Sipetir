import 'package:flutter/material.dart';

class AppColors {
  // Orange tua (card / header)
  static const Color primaryOrange = Color.from(
    alpha: 1,
    red: 0.953,
    green: 0.439,
    blue: 0.129,
  );

  // Orange muda / background
  static const Color softBackground = Color(0xFFFBEDE5);

  // Putih
  static const Color white = Colors.white;
}
